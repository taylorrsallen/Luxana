use crate::*;

////////////////////////////////////////////////////////////////////////////////////////////////////
pub struct LuxanaStatePlugin;
impl Plugin for LuxanaStatePlugin {
    fn build(&self, app: &mut App) {
        app.add_state::<AppState>();
    }
}

////////////////////////////////////////////////////////////////////////////////////////////////////
#[derive(States, Default, Debug, Clone, Copy, PartialEq, Eq, Hash)]
pub enum AppState {
    #[default]
    EngineInit,
    GameInit,
    Main,
}

////////////////////////////////////////////////////////////////////////////////////////////////////
fn sys_update_app_state(
    
) {

}
